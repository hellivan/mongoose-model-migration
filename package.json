{
  "name": "mongoose-model-migration",
  "version": "0.1.5",
  "engines": {
    "node": ">=5.0.0"
  },
  "main": "lib/index.js",
  "license": "MIT",
  "types": "lib/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/hellivan/mongoose-model-migration.git"
  },
  "scripts": {
    "serve": "tsc -w",
    "build": "tsc",
    "start": "node lib/index.js",
    "flow:changelog": "conventional-changelog -p angular -i CHANGELOG.md -s",
    "version": "npm run flow:changelog && git add CHANGELOG.md"
  },
  "dependencies": {
    "@types/mongoose": ">=4.7.3 <6.0.0"
  },
  "devDependencies": {
    "conventional-changelog-cli": "^2.0.5",
    "typescript": "^3.0.3"
  },
  "peerDependencies": {
    "mongoose": ">=4.7.3 <6.0.0"
  }
}
